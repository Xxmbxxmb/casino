export enum Winners {
  DEALER = "DEALER",
  PLAYER = "PLAYER",
  PUSH = "PUSH",
}

export enum Players {
  DEALER = "DEALER",
  PLAYER = "PLAYER",
}

export enum Actions {
  HIT = "hit",
  STAND = "stand",
  DOUBLE = "double",
  SPLIT = "split"
}